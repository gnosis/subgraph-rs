This subdirectory contains helpers to start up a development graph node for
testing the `subgraph-rs` repo.

## Podman Pod File

To start the a development graph node with `podman`:

```
podman play kube docker/pod.yaml
```
